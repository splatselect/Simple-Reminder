<Window x:Class="ReminderApp.ActiveRemindersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Active Reminders"
        Height="500"
        Width="650"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Active Reminders"
                   FontSize="20"
                   FontWeight="Bold"
                   Margin="0,0,0,15"/>

        <Border Grid.Row="1"
                BorderBrush="#CCCCCC"
                BorderThickness="1"
                CornerRadius="5"
                Margin="0,0,0,15">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="RemindersItemsControl" Padding="10">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#F8F9FA"
                                   BorderBrush="#DEE2E6"
                                   BorderThickness="1"
                                   CornerRadius="8"
                                   Padding="15"
                                   Margin="0,0,0,10">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0"
                                             Text="{Binding Message}"
                                             FontSize="15"
                                             FontWeight="SemiBold"
                                             TextWrapping="Wrap"
                                             Foreground="#2C3E50"
                                             Margin="0,0,0,8"/>

                                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
                                        <TextBlock Text="⏰ Due: "
                                                 FontSize="13"
                                                 Foreground="#7F8C8D"/>
                                        <TextBlock Text="{Binding DueTime, StringFormat='{}{0:M/d/yyyy h:mm:ss tt}'}"
                                                 FontSize="13"
                                                 FontWeight="SemiBold"
                                                 Foreground="#3498DB"/>
                                    </StackPanel>

                                    <StackPanel Grid.Row="2" Orientation="Horizontal">
                                        <Button Content="Extend"
                                               Padding="12,6"
                                               Margin="0,0,8,0"
                                               Background="#F39C12"
                                               Foreground="White"
                                               BorderThickness="0"
                                               Click="Extend_Click"
                                               Tag="{Binding}"
                                               Cursor="Hand">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Background" Value="#F39C12"/>
                                                    <Style.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#E67E22"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                            <Button.Template>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}"
                                                           CornerRadius="4"
                                                           Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter HorizontalAlignment="Center"
                                                                        VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Button.Template>
                                        </Button>

                                        <Button Content="Dismiss"
                                               Padding="12,6"
                                               Background="#E74C3C"
                                               Foreground="White"
                                               BorderThickness="0"
                                               Click="Dismiss_Click"
                                               Tag="{Binding}"
                                               Cursor="Hand">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Background" Value="#E74C3C"/>
                                                    <Style.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#C0392B"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                            <Button.Template>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}"
                                                           CornerRadius="4"
                                                           Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter HorizontalAlignment="Center"
                                                                        VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Button.Template>
                                        </Button>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <TextBlock x:Name="CountTextBlock"
                      Text="No active reminders"
                      FontSize="13"
                      Foreground="#7F8C8D"
                      VerticalAlignment="Center"
                      Margin="0,0,15,0"/>
            <Button Content="Settings"
                   Padding="15,8"
                   Margin="0,0,10,0"
                   Click="Settings_Click"/>
            <Button Content="Refresh"
                   Padding="15,8"
                   Margin="0,0,10,0"
                   Click="Refresh_Click"/>
            <Button Content="Close"
                   Padding="15,8"
                   Click="Close_Click"/>
        </StackPanel>
    </Grid>
</Window>
